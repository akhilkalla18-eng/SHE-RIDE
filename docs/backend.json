
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user profile in the SheRide application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user profile, matching Firebase Auth UID."
        },
        "name": {
          "type": "string",
          "description": "User's full name."
        },
        "phoneNumber": {
          "type": "string",
          "description": "User's phone number."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "User's email address."
        },
        "city": {
          "type": "string",
          "description": "User's city of residence."
        },
        "drivingLicenseId": {
          "type": "string",
          "description": "URL to the user's uploaded Driving License ID (optional)."
        },
        "profileVerified": {
          "type": "boolean",
          "description": "Indicates if the user's profile is verified by an admin."
        },
        "emergencyContact": {
          "type": "string",
          "description": "User's emergency contact information."
        }
      },
      "required": ["id", "name", "phoneNumber", "email", "city", "profileVerified"]
    },
    "Ride": {
      "title": "Ride",
      "type": "object",
      "description": "Represents a ride from offer/request to completion.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the ride." },
        "driverId": { "type": "string", "description": "UID of the user who is driving. Null if it's a passenger's open request." },
        "passengerId": { "type": "string", "description": "UID of the user who is the passenger. Null if it's a driver's open offer." },
        "participantIds": { "type": "array", "items": { "type": "string" }, "description": "Array containing the UIDs of the rider and/or the passenger." },
        "status": { "type": "string", "enum": ["offering", "pending", "confirmed", "in-progress", "completed", "cancelled"], "description": "Status of the ride." },
        "sharedCost": { "type": "number", "description": "The agreed shared cost between the rider and passenger." },
        "dateTime": { "type": "string", "description": "ISO string of the confirmed ride's date and time." },
        "createdAt": { "type": "any", "description": "Timestamp of when the ride was created."},
        "acceptedAt": { "type": "any", "description": "Timestamp of when the ride request was accepted by the driver." },
        "fromLocation": { "type": "string", "description": "The starting location of the ride."},
        "toLocation": { "type": "string", "description": "The destination of the ride."},
        "vehicleType": { "type": "string", "enum": ["Bike", "Scooty"], "description": "Type of vehicle (Bike / Scooty)." },
        "rideOtp": { "type": "string", "description": "4-digit OTP for ride verification." },
        "otpVerified": { "type": "boolean", "description": "Indicates if the OTP has been verified by the rider." },
        "riderStarted": { "type": "boolean", "description": "Indicates if the rider has confirmed the ride start." },
        "passengerStarted": { "type": "boolean", "description": "Indicates if the passenger has confirmed the ride start." },
        "riderCompleted": { "type": "boolean", "description": "Indicates if the rider has confirmed the ride completion." },
        "passengerCompleted": { "type": "boolean", "description": "Indicates if the passenger has confirmed the ride completion." },
        "completedAt": { "type": "any", "description": "Timestamp of when the ride was completed." },
        "cancelledBy": { "type": "string", "description": "UID of the user who cancelled the ride." },
        "cancelledAt": { "type": "any", "description": "Timestamp of when the ride was cancelled." }
      },
      "required": ["participantIds", "status", "dateTime", "sharedCost", "fromLocation", "toLocation"]
    },
    "RideRequest": {
      "title": "RideRequest",
      "type": "object",
      "description": "Represents a passenger's request to join a specific driver's offered ride.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the ride request." },
        "rideId": { "type": "string", "description": "The ID of the ride document being requested." },
        "driverId": { "type": "string", "description": "The UID of the driver offering the ride." },
        "passengerId": { "type": "string", "description": "The UID of the passenger making the request." },
        "status": { "type": "string", "enum": ["pending", "accepted", "rejected"], "description": "The status of the request, managed by the driver." },
        "createdAt": { "type": "any", "description": "Timestamp of when the request was made."}
      },
      "required": ["rideId", "driverId", "passengerId", "status", "createdAt"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents a single message within a chat.",
      "properties": {
        "senderId": {
          "type": "string",
          "description": "UID of the user who sent the message."
        },
        "text": {
          "type": "string",
          "description": "The content of the message."
        },
        "createdAt": {
          "type": "any",
          "description": "Timestamp when the message was sent."
        }
      },
      "required": ["senderId", "text", "createdAt"]
    },
    "SosAlert": {
      "title": "SosAlert",
      "type": "object",
      "description": "Represents an SOS alert triggered by a user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the SOS alert." },
        "userProfileId": { "type": "string", "description": "UID of the user who triggered the alert." },
        "rideId": { "type": "string", "description": "Reference to the Ride during which the alert was triggered." },
        "timestamp": { "type": "any", "description": "Timestamp when the alert was triggered." },
        "location": { "type": "string", "description": "Location of the user when the alert was triggered (e.g., coordinates)." }
      },
      "required": ["userProfileId", "rideId", "timestamp", "location"]
    },
    "Notification": {
      "title": "Notification",
      "type": "object",
      "description": "Represents an in-app notification for a user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the notification." },
        "userId": { "type": "string", "description": "UID of the user who will receive the notification." },
        "rideId": { "type": "string", "description": "ID of the related ride, if applicable." },
        "message": { "type": "string", "description": "The content of the notification message." },
        "type": { "type": "string", "enum": ["ride_cancelled", "ride_accepted", "ride_requested", "new_message", "ride_completed"], "description": "The type of notification." },
        "cancelledBy": { "type": "string", "enum": ["passenger", "provider"], "description": "Indicates who cancelled the ride." },
        "isRead": { "type": "boolean", "description": "True if the user has read the notification." },
        "createdAt": { "type": "any", "description": "Timestamp of when the notification was created." }
      },
      "required": ["userId", "message", "type", "isRead", "createdAt"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores public user profile data."
    },
    "/rides/{rideId}": {
      "schema": { "$ref": "#/entities/Ride" },
      "description": "Stores all ride data, from offer/request to completion. The single source of truth for a ride."
    },
    "/rideRequests/{rideRequestId}": {
      "schema": { "$ref": "#/entities/RideRequest" },
      "description": "Stores passenger requests for specific ride offers."
    },
    "/rides/{rideId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/ChatMessage" },
      "description": "Stores chat messages for a specific ride."
    },
    "/sos_alerts/{sosAlertId}": {
      "schema": { "$ref": "#/entities/SosAlert" },
      "description": "Stores SOS alerts triggered by users during rides."
    },
    "/notifications/{notificationId}": {
      "schema": { "$ref": "#/entities/Notification" },
      "description": "Stores in-app notifications for users."
    }
  }
}
